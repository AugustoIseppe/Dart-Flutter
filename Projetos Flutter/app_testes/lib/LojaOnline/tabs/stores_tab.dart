import 'package:flutter/material.dart';

class StoresTab extends StatelessWidget {
  const StoresTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.purple[300],
      child: Center(child: Text("PÁGINA\nLOJAS")),
    );
  }
}